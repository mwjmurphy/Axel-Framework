package org.xmlactions.action.config;

public class TestExecContext {

	// 1) test if we can share maps between ExecContext and ApplicationContext
	// 2) test if we can load languages
	// 3) test if we can StrSubstituor
	// 4) test if we can 
}
