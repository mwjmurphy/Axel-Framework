package org.xmlactions.action.config;

public class TestSessionPersistenceExecContext {

	// 1) test if we can clear persistenceMap
	// 2) test if we can save persistenceMap
	// 3) test if we can reload persistenceMap
}
